[project]
name = "banco-beneficios"
version = "0.1.0"
description = "Scraper de promociones bancarias de Paraguay"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
    "playwright>=1.40.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"